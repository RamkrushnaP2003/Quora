<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All posts</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <h1>Quora Posts</h1>

    <% for(post of posts) { %>
        <div class="posts">
            <h3 class="user">
                @<%= post.username %>
            </h3>
            <p class="content">
                <%= post.content %>
            </p>
            <a href="http://localhost:8080/posts/<%=post.id%>"><button class="view">view</button></a>
            <a href="http://localhost:8080/posts/<%=post.id%>/edit"><button class="update">Update</button></a>
            <form class="form" method="post" action="/posts/<%=post.id%>?_method=DELETE">
                <button class="delete"><i class="fa-sharp fa-solid fa-trash"></i>Delete</button>
            </form>
        </div>
        <% } %>
            <br><br><br>
            <a href="http://localhost:8080/posts/new"><button class="create">Create New Post</button></a>
</body>

</html>